<!DOCTYPE html>
    <html lang="en">
         <head>
            <h1>This is your projects</h1>
        </head>
        <body>
        <nav>
        </nav>
        <div class="container">
            <h1>Projects</h1>
            <ul>
                <% profile.projects.forEach(function(project) { %>
                    <li>
                        <strong><%= project.title %></strong>: $<%= project.description %>
                    </li>
                <% }); %>
            </ul>
            </div>
        </body>
    </html>